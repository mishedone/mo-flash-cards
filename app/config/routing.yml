api:
    resource: "@MoFlashCardsApiBundle/Resources/config/routing.yml"
    type: rest
    prefix: /api

flash_cards:
    resource: "@MoFlashCardsBundle/Resources/config/routing.yml"
    prefix: /
    requirements:
        _locale: en|bg

homepage:
    path: /
    defaults: { _controller: MoFlashCardsBundle:Learn:decks, _locale: bg }
    
logout:
    path: /logout
